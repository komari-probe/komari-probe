# VPS 资源模型与监控指标体系规范 (v1.0)

> **文档定位**：本规范作为纯探针服务端（Server）、探针客户端（Agent）、前端主题（Theme）及告警引擎（Alert）的统一领域模型（Domain Model）与数据契约（Data Contract）。
> 所有数据库设计、API 字段、采集逻辑均应以此文档为基准。

---

## 核心设计哲学：资源监控的二维正交模型

整个探针体系基于 **第一性原理（First Principles）** 进行抽象，构建 **“资源对象 × 监控行为”** 的正交体系：

$$\text{监控体系} = \text{资源维度 (What)} \times \text{监控行为 (How)}$$

- **资源维度（What）**：一台 VPS 所拥有的所有软硬件资产、网络通路及财务属性。
- **监控行为（How）**：
  1. **态势感知（Sensing）**：当前占用、实时速率、状态展示；
  2. **时序追踪（Tracking）**：历史趋势、降采样聚合（Rollup）、波形图与分析；
  3. **守卫告警（Guarding）**：阈值预警、离线熔断、状态恢复通知；
  4. **按需体检（Auditing）**：针对重型指标的按需触发快照与深度诊断。

---

## 维度一：算力资源（Compute Resources）

衡量服务器的核心计算规模、硬件底色与即时运算压力。本维度分为 **CPU（通用计算）** 与 **GPU（加速计算）** 两大支柱。

### 1.1 CPU 静态属性（规格、硬件特征与权威基准性能）

CPU 静态属性的核心目标是**让用户清晰感知该 CPU 到底具备多大算力水平**。设计上采用 **“三层渐进式呈现”**：

#### A. 基础规格层（默认主卡片展示）
| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集机制 | 核心价值与说明 |
| :--- | :--- | :--- | :--- | :--- |
| **CPU 完整型号** | `cpu.model` | String | 读取 `/proc/cpuinfo` | 识别微架构代数与制程工艺（如 AMD Ryzen 9 7950X, Intel Xeon E5-2683 v4） |
| **处理器架构** | `cpu.arch` | String | `uname -m` / 系统调用 | 识别指令架构，如 `x86_64` (amd64), `aarch64` (arm64) |
| **物理核 / 逻辑线程数** | `cpu.cores` / `cpu.threads` | Integer | 读取 `/proc/cpuinfo` | 决定并发任务承载上限（如 1C/1T, 2C/4T, 4C/8T） |
| **标称工作主频** | `cpu.freq_mhz` | Float (MHz / GHz) | 读取 `/proc/cpuinfo` | 衡量单线程基准时钟速率（如 2100.00 MHz / 2.10 GHz） |
| **算力类型标签** | `cpu.allocation_type` | Enum (`dedicated` / `burstable`) | 静态配置 / 标识 | **极高实用价值**：区分是 100% 独享核心（Dedicated），还是积分制突发共享核心（Burstable，超频会限速） |

#### B. 硬件进阶特征层（抽屉 / 硬件详情展示）
| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集机制 | 核心价值与说明 |
| :--- | :--- | :--- | :--- | :--- |
| **三级缓存 (L3 Cache)** | `cpu.cache_l3` | Bytes / MB | `/sys/devices/system/cpu/cpu0/cache/index3/size` | 衡量抗并发与抗缓存抖动能力，母鸡超卖严重时缓存往往被阉割 |
| **硬件解密加速 (AES-NI)** | `cpu.flags.has_aes` | Boolean | 读取 `/proc/cpuinfo` 中的 `flags` (`aes`) | **网络/代理命门**：具备该指令集，TLS/HTTPS 与加密流量传输 CPU 零负担；缺失则容易满载 |
| **硬件虚拟化支持** | `cpu.flags.has_vmx` | Boolean | 读取 `flags` (`vmx` / `svm`) | 决定宿主机是否支持嵌套虚拟化（在 VPS 内跑 KVM 虚拟机或增强 Docker） |
| **高级向量扩展指令** | `cpu.flags.simd` | Array[String] | 读取 `flags` (`avx2`, `avx512` 等) | 决定现代数据库、科学计算、AI 推理的并行处理能力 |

#### C. 权威性能跑分层（【按需主动触发】+【耗时预警】+【结果持久化】）
> ⚠️ **核心安全机制**：探针**绝不在后台静默自动执行重型跑分**（防止突发型 VPS 因持续满载被服务商停机封号）。用户在面板主动点击【开始性能测试】后，弹出明确的耗时与 100% CPU 占用预警，用户二次确认后执行，测试结果**永久固化到该机器档案**。

| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 执行机制与耗时预警 | 核心价值与说明 |
| :--- | :--- | :--- | :--- | :--- |
| **Sysbench 单核/多核算力分** | `cpu.bench.sysbench` | Float (Events/s) | 按需触发（预警：耗时约 10 秒，CPU 100%） | 权威轻量素数运算，量化纯 CPU 运算吞吐 |
| **Geekbench 5/6 单核得分** | `cpu.bench.gb_single` | Integer (Score) | 按需触发（预警：耗时约 2~3 分钟，CPU 100%） | 全球主机圈公认权威的单核算力锚点（500 分老旧，1200 分主流，2000+ 分旗舰） |
| **Geekbench 5/6 多核得分** | `cpu.bench.gb_multi` | Integer (Score) | 按需触发（预警：耗时约 2~3 分钟，CPU 100%） | 全球公认的多任务多核整体性能得分 |
| **同规格机型排位百分比** | `cpu.bench.rank_percent` | Float (%) | 跑分后对比数据库聚合推算 | 直观展示该 VPS 在所有同核心同类机器中的相对档次（如“击败 78% 同配置机型”） |

---

### 1.2 CPU 动态使用情况（即时状态、细分压力与时序指标）

CPU 动态指标的核心目标是**全天候感知算力消耗趋势、捕获隐形卡顿瓶颈，并在发生异常时提供根因定位能力**：

| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集策略与频次 | 数据源 / 获取机制 | 告警支持 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- | :---: | :--- |
| **CPU 总体利用率** | `cpu.usage_percent` | Float (%) | 高频动态（1s ~ 3s） | `/proc/stat` 总体增量计算 | **是** | 最核心大盘指标，持续高于阈值（如 85% 持续 2 分钟）触发告警 |
| **单核利用率分布** | `cpu.core_usages` | Array[Float] (%) | 高频动态（1s ~ 3s） | `/proc/stat` 各 `cpuN` 增量 | **是** | **排查死锁利器**：发现“全局仅 25%，但某一个单核被死循环进程吃满 100%”的隐形瓶颈 |
| **系统平均负载** | `cpu.load_1` / `5` / `15` | Float | 高频动态（1s ~ 3s） | `/proc/loadavg` | **是** | 衡量排队任务等待长度；超过逻辑 CPU 核心总数表示系统任务积压严重 |
| **I/O 等待耗时占比** | `cpu.iowait_percent` | Float (%) | 高频动态（1s ~ 3s） | `/proc/stat` 中的 `iowait` 时间片 | **是** | **硬盘卡顿根因**：CPU 虽空闲，但进程全卡死在等慢速磁盘读写 |
| **CPU 偷窃时间占比** | `cpu.steal_percent` | Float (%) | 高频动态（1s ~ 3s） | `/proc/stat` 中的 `steal` (`%st`) | **是** | **VPS 照妖镜**：母鸡强行剥离当前 VPS 时间片分配给其他租户，大于 5%~10% 说明邻居在抢算力或母鸡超卖严重 |
| **用户态与内核态消耗** | `cpu.user` / `cpu.system` | Float (%) | 高频动态（1s ~ 3s） | `/proc/stat` (`user` / `system`) | 否 | 区分是用户业务程序在计算，还是系统内核调用与驱动在忙 |
| **按需 Top 进程 CPU 快照** | `process.top_cpu` | Array[Process] | **按需触发**（仅在打开面板排查时采样） | 遍历 `/proc/[pid]/stat` 内存排序 | 否 | **告警后的下一问**：“到底是谁在吃 CPU？”按需抓取前 10 名进程（PID、命令名、CPU%），平时 0 开销 |
| **CPU 核心硬件温度** | `cpu.temperature` | Float (°C) | 低频动态（10s） | `/sys/class/thermal/` | **是** | 针对物理独服、家用 NAS、工控机等物理硬件防过热过载保护 |

---

### 1.3 GPU 算力资源（异构加速计算）
> 📌 **状态**：暂缓讨论 / 作为可选专业插件扩展。普通 VPS 通常不配备独显，优先集中精力攻坚通用资源。

---

## 维度二：存储与内存资源（Memory & Storage Resources）

本维度覆盖服务器的两大核心支撑单元：**持久化存储（Storage / 硬盘）** 与 **运行工作集（Memory / 内存）**。

---

### 2.1 存储资源（Storage / 硬盘）

#### 2.1.1 存储静态属性（空间结构、硬件介质与基准性能上限）

存储静态属性的核心目标是**让用户明确“可用空间有多大、分区如何挂载、磁盘介质是什么，以及底层读写吞吐的硬实力”**：

##### A. 空间规格与分区布局层（默认主卡片展示）
| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集机制 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **磁盘总物理容量** | `disk.total_raw` | Bytes / GB | 读取系统块设备总量 | 服务器被分配的全部裸磁盘总容量 |
| **各分区与挂载点布局** | `disk.partitions` | Array[PartitionInfo] | `statfs` 系统调用 / `df` | 罗列所有挂载分区（如 `/`, `/data`, `/boot`）的分配容量与挂载路径 |
| **文件系统类型** | `disk.filesystem` | String | 读取 `/proc/mounts` | 明确分区格式（如 `ext4`, `xfs`, `btrfs`, `zfs`, `ntfs`），影响高级存储特性 |

##### B. 硬件介质与总线特征层（硬件详情展示）
| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集机制 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **存储介质类型** | `disk.media_type` | Enum (`ssd`/`hdd`) | `/sys/block/<dev>/queue/rotational` | **0 开销内核判断**：`0` 表示固态闪存 (SSD/NVMe)，`1` 表示旋转机械盘 (HDD)，面板打上醒目标签 |
| **磁盘总线/驱动类型** | `disk.bus_type` | String | `udev` / 块设备信息 | 识别是原生高速 `Virtio-blk` / `NVMe 直通`，还是传统低效的 `IDE 模拟` |

##### C. 权威 I/O 性能跑分层（【按需主动触发】+【耗时预警】+【结果持久化】）
> ⚠️ **核心安全机制**：探针**绝不在后台静默自动执行 Fio 硬盘压测**（防止因持续高频写入搞坏虚拟磁盘、耗尽云盘 IOPS 突发积分，或被云服务商以“滥用 I/O”封停）。用户点击后弹出明确预警，测试完成后**数据永久固化到该机器档案**。

| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 执行机制与耗时预警 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **4K 随机读写 IOPS** | `disk.bench.fio_4k_iops` | Integer (IOPS) | 按需触发（预警：耗时约 20~30 秒，产生密集磁盘读写） | **系统流畅度生命线**：决定小文件读写、数据库并发、软件包安装是否丝滑 |
| **1M 顺序读写吞吐** | `disk.bench.fio_1m_seq` | Float (MB/s) | 按需触发（预警：耗时约 20~30 秒） | 决定大文件上传下载、打包解压、整机备份的最高速度 |
| **平均 I/O 响应延迟** | `disk.bench.fio_latency` | Float (ms / μs) | 伴随 Fio 测试产出 | 衡量底层存储池的响应延迟（NVMe 通常 < 1ms，机械盘或超卖云盘可能高达 20ms+） |

---

#### 2.1.2 存储动态使用情况（即时容量变化与 I/O 实时流）

存储动态指标的核心目标是**全天候防范磁盘撑爆、预警 INode 耗尽死锁，并实时呈现磁盘 I/O 吞吐水流与繁忙瓶颈**：

| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集策略与频次 | 数据源 / 获取机制 | 告警支持 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- | :---: | :--- |
| **各分区已用 / 剩余容量** | `disk.used` / `disk.free` | Bytes / GB | 低频动态（30s ~ 60s） | `statfs` 系统调用 | **是** | 监控实际空间消耗，超过安全线（如使用率 >90%）立即告警，防止服务宕机 |
| **分区空间利用率** | `disk.usage_percent` | Float (%) | 低频动态（30s ~ 60s） | 计算 `used / total * 100` | **是** | 大盘核心警示指标，标红展示快满的分区 |
| **INode 索引节点使用率** | `disk.inode_percent` | Float (%) | 低频动态（30s ~ 60s） | `statfs` 结构中的 `f_ffree` / `f_files` | **是** | **排查隐形假满神器**：防止产生海量零碎小文件（如缓存、邮件队列）导致“磁盘有剩余却无法创建新文件”的诡异宕机 |
| **磁盘实时读取速率** | `disk.read_bps` | Bytes/s | 高频动态（1s ~ 3s） | `/proc/diskstats` 差值 | **是** | 监控当前瞬时读取流量，判断是否有大数据加载或全盘扫描 |
| **磁盘实时写入速率** | `disk.write_bps` | Bytes/s | 高频动态（1s ~ 3s） | `/proc/diskstats` 差值 | **是** | 监控当前瞬时写入流量，判断是否有异常刷盘、备份或日志泛滥 |
| **实时读写 IOPS 计数** | `disk.read_iops` / `write_iops` | Integer (IOPS) | 高频动态（1s ~ 3s） | `/proc/diskstats` 操作数差值 | 否 | 反映当前每秒实际发生的读写操作频次，配合带宽评估负载 |
| **磁盘繁忙度百分比** | `disk.busy_percent` (`%util`) | Float (%) | 高频动态（1s ~ 3s） | `/proc/diskstats` 活跃时间比 | **是** | 类似 `iostat -x` 里的 `%util`；持续 100% 意味着磁盘 I/O 完全饱和，成为系统全盘卡顿的总瓶颈 |
| **按需 Top 刷盘进程快照** | `process.top_disk` | Array[Process] | **按需触发**（仅在打开面板排查时采样） | 结合 `/proc/[pid]/io` 内存排序 | 否 | **告警后的下一问**：“到底是谁在疯狂刷盘？”按需抓取前 5 名写入最猛的进程，平时 0 开销 |

---

### 2.2 内存资源（Memory / 物理内存与 Swap）

#### 2.2.1 内存静态属性（空间分配、超开特征与读写吞吐上限）

内存静态属性的核心目标是**让用户明确“物理与交换内存的真实分配规模、是否存在被母鸡动态抽走内存的通道，以及内存总线的吞吐硬实力”**：

##### A. 基础容量与交换空间层（默认主卡片展示）
| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集机制 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **物理内存总容量 (RAM Total)** | `mem.total` | Bytes / MB / GB | 读取 `/proc/meminfo` (`MemTotal`) | 机器被分配的真实物理运行内存总量 |
| **交换分区总容量 (Swap Total)** | `swap.total` | Bytes / MB / GB | 读取 `/proc/meminfo` (`SwapTotal`) | **低配机兜底分水岭**：在 512M/1G 小鸡上，是否有 Swap 决定了高负荷时是程序瞬间被 OOM 强杀，还是降速抗住 |

##### B. 虚拟化超开特征层（硬件详情展示 —— 【VPS 核心专属】）
| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集机制 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **气球回收驱动状态 (Ballooning)** | `mem.ballooning_detected` | Boolean | 检查内核 `virtio_balloon` 模块状态 | **防母鸡抽内存**：部分不良商家悄悄加载气球驱动，当母鸡内存紧张时把虚机可用内存强行抽走；面板打上警示标识 |

##### C. 权威内存带宽基准层（【按需主动触发】+【耗时预警】+【结果持久化】）
> ⚠️ **核心安全机制**：探针**绝不在后台静默自动执行内存压测**。用户在面板主动点击【测试内存吞吐】后，耗时仅需 **2~3 秒**，测完后**结果永久固化到该机器档案**。

| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 执行机制与耗时预警 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **内存读取带宽** | `mem.bench.read_bandwidth` | Float (MB/s / GB/s) | 按需触发（预警：耗时约 2 秒，轻量内存读写） | 正常 DDR4 约 15~25 GB/s；低于 5 GB/s 说明母鸡内存总线被极其严重地抢占 |
| **内存写入带宽** | `mem.bench.write_bandwidth` | Float (MB/s / GB/s) | 按需触发（预警：耗时约 2 秒） | 衡量内存连续高频写入吞吐能力 |
| **内存纳秒级延迟** | `mem.bench.latency` | Float (ns) | 伴随 Sysbench 内存测试产出 | 衡量内存寻址纳秒级延迟（如 `225 ns`） |

---

#### 2.2.2 内存动态使用情况（工作集消耗、真实可用与换页压力）

内存动态指标的核心目标是**严格区分真实可用与文件缓存以杜绝误报、预警 Swap 剧烈换页导致的系统颠簸假死，并捕获内核静默 OOM 强杀事件**：

| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集策略与频次 | 数据源 / 获取机制 | 告警支持 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- | :---: | :--- |
| **真实可用物理内存** | `mem.available` | Bytes / MB / GB | 高频动态（1s ~ 3s） | `/proc/meminfo` (`MemAvailable`) | **是** | **核心生命线**：剔除不可回收页后内核能真正立即腾出的内存容量，低于警戒线（如 100MB）立即告警 |
| **物理内存实际已用** | `mem.used` | Bytes / MB / GB | 高频动态（1s ~ 3s） | `Total - Available` | 否 | 排除掉随时可丢弃的文件缓存后，应用程序真正占用的工作集总量 |
| **真实内存利用率** | `mem.usage_percent` | Float (%) | 高频动态（1s ~ 3s） | `(Total - Available) / Total * 100` | **是** | 大盘主看板核心指标，真实反映物理内存消耗压力，避免传统 `Total - Free` 假满误报 |
| **文件缓存与缓冲池** | `mem.cache_buffers` | Bytes / MB / GB | 低频动态（15s ~ 30s） | `Cached + Buffers` | 否 | 呈现操作系统借用了多少物理内存作为 IO 磁盘读写加速 |
| **Swap 空间已用与占比** | `swap.used` / `swap.usage_percent` | Bytes / Float (%) | 高频动态（1s ~ 3s） | `/proc/meminfo` (`SwapFree`) | **是** | 监控交换分区消耗，Swap 占用持续走高说明物理内存已严重透支，接近宕机边缘 |
| **Swap 换页活动率 (颠簸预警)** | `swap.io_rate` | Bytes/s 或 pgs/s | 高频动态（1s ~ 3s） | `/proc/vmstat` (`pswpin`/`pswpout` 差值) | **是** | **防止整机假死神器**：检测内存颠簸（Thrashing），一旦剧烈换页预示整机 I/O 将被卡死 |
| **内核 OOM 强杀事件** | `mem.oom_kills` | Integer (累计/增量) | 高频事件捕获（1s ~ 3s） | `/proc/vmstat` (`oom_kill`) | **是** | **破案神器**：当程序突然闪退时，第一时间向管理员报警“内核刚刚强杀了进程”，附带时间戳 |
| **按需 Top 内存进程快照** | `process.top_mem` | Array[Process] | **按需触发**（仅在打开面板或告警排查时采样） | 遍历 `/proc/[pid]/statm` (RSS) | 否 | 平时 0 额外 CPU/IO 开销；告警时一键抓出是哪个进程（PID、占用 MB、命令行）吃光了内存 |

---

## 维度三：网络资源（Network Resources —— 最核心变量）

网络具有“空间拓扑非对称性”与“极高的波动敏感度”，是 VPS 最核心也最易踩坑的资源。必须严格规避**虚拟网卡混杂导致的流量翻倍虚报**与**静默自动测速吞噬巨额流量**，并拆分为 **“静态规格与计费配置”**、**“量的监控（流量/带宽）”** 与 **“质的监控（延迟/丢包/TCP重传/路由）”** 三大体系。

### 3.1 静态规格与计费规则配置（Network Spec & Billing Config）

| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集 / 配置机制 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **主公网网卡接口与速率** | `net.interface` / `speed` | String / Mbps | 读取 `/sys/class/net/<dev>/speed` | 识别主通信网卡（如 `eth0`、`ens3`）及其协商物理速率（如 `1000 Mbps`、`10 Gbps`） |
| **虚拟与内部网卡过滤清单** | `net.ignored_interfaces` | Array[String] | 探针内核自动正则识别 | 自动排除 `lo`, `docker*`, `veth*`, `br-*`, `tun*` 等内部虚拟借道设备，杜绝流量重复核算 |
| **商家扣费计算模型** | `net.billing_model` | Enum | 用户设定 (`in_and_out` / `out_only` / `max`) | **还原真实账单**：双向求和、仅计出站、或取入出较大者，与各厂商结算规则完全对齐 |
| **月度流量配额总额** | `net.quota_total` | Bytes / GB / TB | 用户按套餐设定（如 1000 GB, 或无限制） | 流量防超额管理的基础基准 |
| **每月流量清零重置日** | `net.billing_reset_day` | Integer (1~31) | 用户设定（自然月 1 号或购机账单日） | 触发当月流量归档与清零的时间锚点 |
| **权威峰值带宽跑分** | `net.bench.speedtest` | Object (Up/Down/Ping) | **【按需主动触发】**（预警：产生约 1~2GB 测速流量） | 测定当前机器实际能跑满的下行/上行极限带宽，测完后永久固化到卡片档案中 |

### 3.2 量的监控：流量与带宽水流（Traffic & Bandwidth Metrics）

| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集策略与频次 | 数据源 / 获取机制 | 告警支持 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- | :---: | :--- |
| **即时下行速率 (Download)** | `net.rx_bps` | Bytes/s (Mbps) | 高频动态（1s ~ 3s） | `/proc/net/dev` 差值 | **是** | 呈现即时流入流量水流；突发高带宽可设告警防被恶意刷流 |
| **即时上行速率 (Upload)** | `net.tx_bps` | Bytes/s (Mbps) | 高频动态（1s ~ 3s） | `/proc/net/dev` 差值 | **是** | 呈现即时流出流量水流；流出激增可能被黑客发包或遭外发滥用 |
| **当月计费累计流量** | `net.period_billed` | Bytes / GB | 探针上报增量汇总 | 服务端累加存盘 | **是** | 根据所选计费模型准确折算出的当前账单周期已消耗总额，探针重启不丢数 |
| **流量配额消耗百分比** | `net.quota_used_percent` | Float (%) | 动态折算 | `billed / quota_total * 100` | **是** | **防超额巨额账单**：支持 80%、90%、95% 阶梯预警，防止欠费停机或高额扣费 |
| **配额耗尽倒计时预估** | `net.quota_estimated_days`| Float (天) | 动态推算 | 按过去 7 天日均消耗推算 | 否 | 智能提示：“按当前消耗速率，剩余流量预计在 6.5 天后耗尽” |
| **按需活跃连接与对端快照** | `net.top_connections` | Array[Conn] | **按需触发**（平时 0 开销） | 抓取当前并发与流量前 5 名对端 IP | 否 | 突发大流量报警时一键打开排查，看清“到底是谁在疯狂消耗带宽” |

### 3.3 质的监控：延迟、丢包、TCP重传与回程路由（Latency, Route & Quality）

| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集策略与频次 | 数据源 / 获取机制 | 告警支持 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- | :---: | :--- |
| **三网骨干节点往返延迟 (RTT)** | `net.ping.ct` / `cu` / `cm` | Float (ms) | 常态时序（30s ~ 60s） | 向电信/联通/移动代表性节点 ICMP 探测 | **是** | 绘制 24 小时延迟时序图，直观展现每晚 20:00~24:00 晚高峰骨干网延迟跳涨 |
| **丢包率 (Packet Loss)** | `net.ping.loss_percent` | Float (%) | 常态时序（30s ~ 60s） | 周期发包成功率统计 | **是** | **网络核心痛点**：断崖式丢包（如 >15%）立即告警 |
| **网络抖动 (Jitter)** | `net.ping.jitter` | Float (ms) | 常态时序（30s ~ 60s） | 连续报文 RTT 差值平均 | 否 | 评估网络波动平稳度，游戏、VoIP 与实时通信关键体验指标 |
| **系统级 TCP 重传率** | `net.tcp.retrans_rate` | Float (%) | 高频动态（3s ~ 5s） | `/proc/net/snmp` (`RetransSegs`) | **是** | **0 开销内核照妖镜**：突破 ICMP 假象，真实反映当前所有对外连接的丢包重传比例 |
| **三网回程 AS 骨干链** | `net.route.backbone_as` | String / Array | 低频周期 / 按需体检 | NextTrace 轻量追踪 | 否 | 识别三大运营商回程线路属性（163/CN2/4837/9929/CMI） |
| **劣质/绕路检测标记** | `net.route.is_suboptimal` | Boolean | 路由追踪结果比对 | 拓扑地理异常识别 | **是** | 识别例如“香港小鸡回程绕美”、“日本小鸡绕德”等服务商暗中劣化行为 |

---

## 维度四：IP 身份与声誉资源（IP Identity & Reputation）

在现代云服务器与 VPS 场景中，公网 IP 地址不仅是寻址标识，更是**服务器的“网络身份证”、“商业牌照”与“信誉档案”**。本维度分为四大清晰层次：

### 4.1 基础网络身份与地理归属（“这个 IP 是谁？在哪里？”）
| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集机制 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **公网 IPv4 / IPv6** | `ip.v4` / `ip.v6` | String | 外部 STUN / IP 反查接口 | 确认机器是否具备双栈网络、纯 IPv6 还是 NAT 共享 IP；IP 变动（动态漂移/DDNS）时触发告警 |
| **地理归属国家与城市** | `ip.country` / `ip.city` | String | GeoIP2 / MMDB 离线库 | 显示 IP 物理定位（如 `[SG] 新加坡`、`[US] 洛杉矶`），前台展示国旗徽章 |
| **自治系统 (ASN) 与组织** | `ip.asn` / `ip.org` | String | BGP.tools / IPinfo | 明确 IP 属于哪家云厂商或机房（如 `AS209699 Coaxial Cable`，`AS13335 Cloudflare`） |
| **原生 IP vs 广播 IP** | `ip.type_broadcast` | Enum (`native`/`anycast`) | BGP 宣告与延迟拓扑推算 | **核心成色指标**：原生 IP 注册地与物理使用地一致，在各类风控系统中信誉显著优于跨国广播 IP |

### 4.2 商业属性分类（“它是机房还是家宽？”）
| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集机制 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **IP 商业分类** | `ip.usage_type` | Enum (`datacenter`/`residential`) | IP2Location / IPinfo 商业库 | **信任权重**：区分机房机（Hosting/Data Center）还是稀有住宅家宽（Residential）；家宽 IP 极受各大平台信任，极少弹验证码 |

### 4.3 安全信誉与风险评分（“这个 IP 干净吗？有没有被黑名单拉黑？”）
| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集机制与频次 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **欺诈风险分 (Fraud Score)** | `ip.fraud_score` | Integer (0 ~ 100) | Scamalytics / IPQS 接口 | **纯净度金标准**：0~10 分为极低风险白名单；>50 分意味着已被大量网站拦截、频繁弹 Google 九宫格验证码 |
| **AbuseIPDB 滥用举报记录** | `ip.abuse_reports` | Integer (Count) | AbuseIPDB 数据库查询 | 检查该 IP 历史上有无扫描、发垃圾邮件、暴力破解或黑客攻击前科 |
| **邮件 25 端口出站状态** | `ip.port25_outbound` | Boolean | 探测外网各大邮箱 SMTP 连通性 | 很多云厂商底层封死 25 端口，该状态决定了用户能否自建私有邮件服务器 |

### 4.4 业务准入与解锁评级（“这个 IP 能用来干嘛？” —— 极客/出海核心刚需）
> 📌 **采集方式**：按需体检或低频更新（如每周/每月一次），防止高频探测导致目标平台封禁。

| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集机制 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **ChatGPT / OpenAI 解锁** | `ip.unlock.chatgpt` | Enum (`native`/`cf_challenge`/`blocked`) | 模拟握手检测 | 现代生产力 VPS 核心指标：区分是原生直接使用、触发 Cloudflare 验证盾，还是被彻底拒绝访问 |
| **Claude 平台准入** | `ip.unlock.claude` | Enum (`allowed`/`blocked`) | 握手验证状态 | Claude 风控极其严格，脏 IP 秒封号；该指标是高端出海节点的核心凭证 |
| **主流流媒体解锁评级** | `ip.unlock.streaming` | Map[Service, Status] | 模拟流媒体版权接口探测 | 评估 Netflix、Disney+、YouTube Premium、TikTok 的原生版权解锁能力 |

---

## 维度五：系统与环境资源（OS & Runtime Environment）

衡量服务器的操作系统底座、虚拟化层级、网络协议栈调优及系统开机稳定性。本维度坚决不涉足用户具体业务应用（**不侵入监控 Docker 容器**），保持纯探针绝对纯粹。

### 5.1 系统底座与虚拟化架构（静态特征）
| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集机制 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **操作系统与发行版** | `os.distro` / `os.version` | String | 读取 `/etc/os-release` | 识别宿主机系统（如 `Debian 12`, `Ubuntu 24.04`, `Windows Server 2022`, `Alpine` 等） |
| **Linux 内核版本** | `os.kernel` | String | `uname -r` 系统调用 | 评估底层安全补丁与前沿网络特性支持（如 eBPF, WireGuard 原生支持） |
| **虚拟化架构分级** | `os.virtualization` | Enum (`kvm`/`lxc`/`openvz`/`baremetal`) | `systemd-detect-virt` / `/proc/cpuinfo` | **小鸡血统标识**：明确区分 **KVM 独立架构**（独占内核、可自装BBR、可换内核）与 **LXC/OpenVZ 容器化架构**（共享母鸡内核，受限较多） |
| **TCP 拥塞算法与排队策略** | `os.tcp_congestion` / `qdisc` | String | 读取 `/proc/sys/net/ipv4/tcp_congestion_control` | 检查是否已开启网络加速利器（如 `bbr + fq`）；未开启时面板友好提示用户进行网络调优 |

### 5.2 运行稳定性与意外重启守卫（动态指标）
| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 采集策略与频次 | 告警支持 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :---: | :--- |
| **连续开机运行时长 (Uptime)** | `os.uptime` | Seconds / Days | 高频动态（1s ~ 3s），读 `/proc/uptime` | 否 | 展现服务器运行稳定度与骄傲的持续在线时长 |
| **非预期意外重启告警** | `os.event.reboot` | Event Trigger | 上报 Uptime 出现剧烈倒退/归零时触发 | **是** | **高价值哨兵指标**：当服务器发生母鸡断电、硬件故障、内核崩溃（Kernel Panic）或服务商半夜偷偷重启机器时，第一时间推送紧急告警通知！ |

---

## 维度六：资产与财务资源（Financial & Lifecycle Assets）

从财务成本、合同周期和数字资产管理角度衡量 VPS 的长期持有价值与生命周期。本维度**探针端 0 开销**，核心配置由用户录入维护，衍生价值、到期倒计时与机队总账由服务端全自动推演。

### 6.1 财务与合约静态属性（面板录入/维护项）

| 指标中文名 | 标识符 (Key) | 数据类型 | 典型取值示例 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :--- |
| **服务提供商名称** | `asset.provider` | String | `BandwagonHost`, `DMIT`, `Oracle Cloud`, `RackNerd`, `自建机房` | 明确机器所属 IDC 商家或云厂商 |
| **购买/续费币种** | `asset.currency` | Enum | `USD`, `CNY`, `EUR`, `GBP`, `JPY` | 标记原始结算货币 |
| **续费价格 (原币)** | `asset.renewal_price` | Float | `15.00`, `0.00` (免费机) | 每次续费需缴纳的真实金额（扣除循环优惠码后的净价） |
| **计费结算周期** | `asset.billing_cycle` | Enum | `月付`, `季付`, `半年付`, `年付`, `三年付`, `永久免费`, `一次性买断` | 决定生命周期推演跨度与成本摊销公式 |
| **服务到期绝对时间** | `asset.expired_at` | Timestamp | `2027-06-18 00:00:00` | 商家后台显示的到期日（或下次扣费日） |
| **续费策略与意向** | `asset.renewal_mode` | Enum | `自动扣费` (绑卡), `手动充值`, `到期弃坑` (玩完即扔) | 标为“到期弃坑”的机器不触发紧急夺命连环告警 |
| **首次购入时间** | `asset.created_at` | Date | `2023-11-25` | 记录这台老母鸡陪伴用户多少天（数字资产情怀与保修依据） |

### 6.2 动态衍生指标与智能告警（服务端自动推算）

| 指标中文名 | 标识符 (Key) | 数据类型与单位 | 计算逻辑 / 触发规则 | 告警支持 | 核心价值与业务说明 |
| :--- | :--- | :--- | :--- | :---: | :--- |
| **服务到期倒计时** | `asset.days_left` | Integer (天) | `(expired_at - now) / 86400` | 否 | 直观展示在机器主卡片上（如“剩余 18 天”、“已过期”） |
| **到期防失联阶梯告警** | `asset.event.expiry_alert` | Event Trigger | 距离到期 **15天 / 7天 / 3天 / 1天** 触发 | **是** | **保命神器**：防止用户因日常忙碌或邮箱拦截漏看账单，导致服务商直接清退销毁数据 |
| **日均持有成本** | `asset.daily_cost` | Float (原币/天) | `续费价格 / 该周期实际总天数` | 否 | 算清账本：例如年付 $24 小鸡，日均仅 `$0.065`（约合 0.47 元/天） |
| **二手剩余价值折算** | `asset.remaining_value` | Float (原币) | `(剩余天数 / 周期天数) * 续费价格` | 否 | **主机圈二手出鸡/收鸡标准公允价**：转让出机时直接截图，杜绝争议 |
| **已持有总时长** | `asset.holding_days` | Integer (天) | `(now - created_at) / 86400` | 否 | 记录“已稳定运行 680 天”，展现机器长跑生命力 |

### 6.3 全机队资产聚合大盘（Fleet Financial Overview）

当用户纳管多台机器时，主看板顶部支持全局资产聚合与财务大盘：
1. **机队月度总持有支出（统一汇率折算）**：将美元、欧元、人民币按预设汇率统一换算，直观呈现“全机队 12 台 VPS，月均总支出：¥246.50”。
2. **续费时间线日历（Renewal Timeline）**：直观展示未来 1~3 个月内各机器的到期节点，防止多台机器集中扣费或漏缴。
3. **单核算力 / 每 GB 内存综合单价**：汇总机队总核数、总内存，展示平均单位硬件资源的月度成本，让性价比一目了然。

---

## 预留讨论清单（后续细化讨论切入点）

在后续具体开发与重构中，我们将对上述每个维度的以下关键问题逐一敲定：
1. **指标采集开销边界**：哪些指标必须全天候常态化记录？哪些严格限制为按需查看（On-Demand）？
2. **告警防抖机制**：高负载和丢包如何避免“网络抖一下，手机响十遍”（如持续 3 次周期超标才报）？
3. **隐私脱敏规则**：公开状态页（Status Page）模式下，哪些敏感指标（IP、备注、容器名）必须强制或可选打码？
4. **探针端轻量实现**：在 Linux 与 Windows 双端如何用最少的系统调用把指标拉取出来？
