name: Restore frontend theme
description: Download and unpack a previously built frontend theme artifact.

inputs:
  artifact-name:
    description: Frontend theme artifact name.
    default: sonar-frontend

runs:
  using: composite
  steps:
    - name: Download frontend artifact
      uses: actions/download-artifact@v4
      with:
        name: ${{ inputs.artifact-name }}
        path: ${{ runner.temp }}/frontend-artifact

    - name: Extract frontend artifact
      shell: bash
      run: |
        set -euxo pipefail
        mkdir -p internal/platform/frontend/defaultTheme
        rm -rf internal/platform/frontend/defaultTheme/*
        tar -xzf "$RUNNER_TEMP/frontend-artifact/${{ inputs.artifact-name }}.tar.gz" -C internal/platform/frontend/defaultTheme
        test -s internal/platform/frontend/defaultTheme/dist.tar.zst
